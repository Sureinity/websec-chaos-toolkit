# Config Directory

This directory exists in the bootstrap scaffold for future local configuration
fragments, tool-specific templates, and environment-specific overrides.

Current behavior:

- the scaffolded CLI does not read this directory yet
- the repository-level placeholder files remain `apps.yaml`,
  `pentest-profiles.yaml`, and `chaos-profiles.yaml`
