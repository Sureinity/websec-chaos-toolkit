[pytest]
minversion = 8.0
testpaths = tests
addopts = -ra
markers =
    integration: marks end-to-end or cross-module tests
    external_tools: marks tests that require third-party binaries or live services
