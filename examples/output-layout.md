# Planned Output Layout

Each run should eventually produce artifacts in a stable directory shape.

```text
outputs/
  <run-id>/
    raw/
      <tool-name>/
    normalized/
      findings.json
    reports/
      executive-summary.md
```

This file is documentation only. The bootstrap scaffold does not create run
artifacts yet.
